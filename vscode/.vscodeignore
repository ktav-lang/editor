.vscode/
.gitignore
.gitkeep
**/.gitkeep
node_modules/
out/**/*.map
src/**
tsconfig.json
scripts/
scripts/sync-grammars.js
**/*.ts
!out/**/*.js
out/test/**

# ---------------------------------------------------------------------------
# Per-platform native binaries
# ---------------------------------------------------------------------------
# Include all per-platform native binaries in the VSIX
# (vscode-languageclient will select the right one at runtime via discovery.ts)
